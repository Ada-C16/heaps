

def heap_sort(list):
    """ This method uses a heap to sort an array.
        Time Complexity:  ?
        Space Complexity: ?
    """
    if len(list) <= 1:  # returns list of empty or only has one node
        return list
